export function Verify(condition, errText) {
    if (typeof condition !== "boolean") {
        throw new TypeError('conditions must be typeof boolean');
    }
    if (condition !== true) {
        throw new Error(errText || "");
    }
}
